# Contributing

1. Install Node.js 20 or newer.
2. Run `npm install` in `plugin/`.
3. Start the development server with `npm run dev`.
4. Before submitting a change, run `npm run build`.

Do not commit files from `canvas-data/`. Canvas snapshots may contain embedded images, filenames, and private annotations.
