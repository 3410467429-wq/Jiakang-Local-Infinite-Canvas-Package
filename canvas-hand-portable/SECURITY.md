# Security Policy

Canvas Hand runs a local web service bound to `127.0.0.1` and stores canvas data under the current user's profile. Do not expose the service through a public network proxy.

Do not commit `canvas.json`, `marked.png`, generated images, API keys, or personal annotations. To report a vulnerability, open a GitHub issue without including sensitive data and ask the maintainer for a private contact channel.
